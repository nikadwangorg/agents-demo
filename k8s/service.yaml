apiVersion: v1
kind: Service
metadata:
  name: okr-management-service
  labels:
    app: okr-management
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: okr-management
